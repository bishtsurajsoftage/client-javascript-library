name: Code Scanning

on:
  push:
    branches:
      - main

jobs:
  code-scanning:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Initialize CodeQL
        uses: github/codeql-action/security-experimental

      - name: Code Scanning
        uses: github/codeql-action/security-extented
